---
title: "Determinantes de los Ingresos Familiares mediante Regresión Lineal Múltiple: Un Estudio Empírico con Microdatos de la ECV DANE 2024"
author: "Barros Rayo Alejandro (2415837), Muñoz Portela Diego Fernando (2415620), Portilla Aguirre Johan Camilo (2422468), Aguirre Aldana Joan Sebastian (2419550)"
date: "`r format(Sys.Date(), '%d de %B de %Y')`"
output: 
  html_document:
    theme: lumen
    highlight: textmate
    toc: true
    toc_float:
      collapsed: false
      smooth_scroll: true
    toc_depth: 3
    number_sections: true
    code_folding: show
---

<style type="text/css">
/* RESET COMPLETO */
* {
    box-sizing: border-box !important;
}

/* PALETA DE COLORES ELEGANTE */
:root {
  --primary-color: #1a365d;
  --secondary-color: #2d3748;
  --accent-color: #2b6cb0;
  --accent-light: #4299e1;
  --text-color: #2d3748;
  --light-bg: #f7fafc;
  --border-color: #e2e8f0;
  --shadow-light: rgba(0,0,0,0.06);
  --sidebar-width: 300px;
}

/* ESTRUCTURA PRINCIPAL CON SIDEBAR */
.main-container {
    max-width: 1400px !important;
    margin: 0 auto !important;
    font-family: "Georgia", "Palatino Linotype", "Book Antiqua", serif !important;
    line-height: 1.75 !important;
    color: var(--text-color) !important;
    background: #ffffff !important;
    padding: 20px !important;
    display: flex !important;
    gap: 30px !important;
    align-items: flex-start !important;
    position: relative !important;
}

/* CONTENEDOR DEL ÍNDICE LATERAL - STICKY CORREGIDO */
#TOC {
    background: white !important;
    border: 1px solid var(--border-color) !important;
    border-radius: 8px !important;
    padding: 25px !important;
    box-shadow: 0 2px 15px var(--shadow-light) !important;
    font-family: "Times New Roman", serif !important;
    width: var(--sidebar-width) !important;
    min-width: var(--sidebar-width) !important;
    max-width: var(--sidebar-width) !important;
    position: sticky !important;
    top: 20px !important;
    overflow-y: auto !important;
    overflow-x: hidden !important;
    flex-shrink: 0 !important;
    margin: 0 !important;
    max-height: calc(100vh - 40px) !important; /* Altura máxima viewport */
    align-self: flex-start !important;
    transition: all 0.3s ease !important;
}

/* Scrollbar personalizado para el TOC */
#TOC::-webkit-scrollbar {
    width: 6px !important;
}

#TOC::-webkit-scrollbar-track {
    background: #f1f1f1 !important;
    border-radius: 3px !important;
}

#TOC::-webkit-scrollbar-thumb {
    background: var(--accent-light) !important;
    border-radius: 3px !important;
}

#TOC::-webkit-scrollbar-thumb:hover {
    background: var(--accent-color) !important;
}

/* Reset completo para elementos del TOC */
#TOC * {
    box-sizing: border-box !important;
    max-width: 100% !important;
    overflow: visible !important;
}

#TOC::before {
    content: "📋 Índice de Contenidos" !important;
    display: block !important;
    font-family: "Times New Roman", serif !important;
    font-size: 1.3em !important;
    font-weight: bold !important;
    color: var(--primary-color) !important;
    border-bottom: 2px solid var(--accent-color) !important;
    padding-bottom: 12px !important;
    margin-bottom: 20px !important;
    text-align: left !important;
    position: sticky !important;
    top: 0 !important;
    background: white !important;
    z-index: 2 !important;
    margin-top: 0 !important;
}

/* LISTAS DEL ÍNDICE CON SCROLL INTERNO */
#TOC ul {
    list-style: none !important;
    padding-left: 0 !important;
    margin: 0 !important;
    width: 100% !important;
    overflow: visible !important;
}

#TOC li {
    margin: 6px 0 !important;
    padding: 0 !important;
    line-height: 1.5 !important;
    width: 100% !important;
    overflow: visible !important;
}

#TOC a {
    color: var(--text-color) !important;
    text-decoration: none !important;
    font-size: 0.92em !important;
    display: block !important;
    padding: 8px 12px !important;
    border-left: 3px solid transparent !important;
    transition: all 0.3s ease !important;
    border-radius: 4px !important;
    width: 100% !important;
    white-space: normal !important;
    word-wrap: break-word !important;
    overflow-wrap: break-word !important;
}

#TOC a:hover {
    color: var(--accent-color) !important;
    background-color: var(--light-bg) !important;
    border-left: 3px solid var(--accent-color) !important;
    padding-left: 15px !important;
    transform: translateX(2px) !important;
}

#TOC .toc-section-number {
    color: var(--accent-color) !important;
    font-weight: 600 !important;
    margin-right: 6px !important;
    display: inline-block !important;
    min-width: 18px !important;
    font-size: 0.9em !important;
}

/* SUBNIVELES DEL ÍNDICE */
#TOC ul ul {
    padding-left: 18px !important;
    margin-top: 4px !important;
    width: 100% !important;
}

#TOC ul ul a {
    font-size: 0.85em !important;
    color: #666666 !important;
    border-left: 2px solid var(--border-color) !important;
    padding: 6px 10px !important;
    width: calc(100% - 0px) !important;
}

#TOC ul ul a:hover {
    color: var(--accent-color) !important;
    border-left: 2px solid var(--accent-light) !important;
    background-color: #f8f9fa !important;
}

/* CONTENIDO PRINCIPAL (LADO DERECHO) */
.toc-content {
    flex: 1 !important;
    margin-left: 0 !important;
    padding: 40px !important;
    background: white !important;
    box-shadow: 0 0 20px var(--shadow-light) !important;
    border-radius: 8px !important;
    min-width: 0 !important;
}

.section.level1 {
    margin-bottom: 50px !important;
    padding: 35px !important;
    background: white !important;
    border: 1px solid var(--border-color) !important;
    border-radius: 8px !important;
    box-shadow: 0 2px 8px var(--shadow-light) !important;
    width: 100% !important;
}

/* TIPOGRAFÍA */
h1, h2, h3, h4, h5, h6 {
    font-family: "Times New Roman", "Times", serif !important;
    font-weight: 600 !important;
    text-align: left !important;
}

h1 {
    color: var(--primary-color) !important;
    border-bottom: 3px double var(--accent-color) !important;
    padding-bottom: 20px !important;
    font-size: 2.4em !important;
    margin-bottom: 35px !important;
    text-align: center !important;
    letter-spacing: 0.5px !important;
    font-weight: 700 !important;
}

h2 {
    color: var(--primary-color) !important;
    border-bottom: 2px solid var(--accent-light) !important;
    padding-bottom: 12px !important;
    font-size: 1.9em !important;
    margin-top: 40px !important;
    margin-bottom: 25px !important;
    padding-left: 15px !important;
    border-left: 4px solid var(--accent-color) !important;
}

h3 {
    color: var(--secondary-color) !important;
    font-size: 1.5em !important;
    margin-top: 30px !important;
    margin-bottom: 18px !important;
    padding-left: 10px !important;
    border-left: 3px solid var(--border-color) !important;
}

/* JUSTIFICACIÓN DE TEXTO */
p, div, .section, .main-container, li, td {
    text-align: justify !important;
    text-justify: inter-word !important;
    hyphens: auto !important;
}

/* ABSTRACT */
.abstract {
    background: linear-gradient(135deg, var(--light-bg) 0%, #edf2f7 100%) !important;
    padding: 30px 35px !important;
    border-left: 5px solid var(--accent-color) !important;
    margin-bottom: 35px !important;
    font-style: normal !important;
    color: var(--text-color) !important;
    text-align: justify !important;
    font-size: 1.1em !important;
    line-height: 1.8 !important;
    border-radius: 0 8px 8px 0 !important;
}

/* TABLAS */
table {
    width: 100% !important;
    border-collapse: collapse !important;
    margin: 25px 0 !important;
    font-family: "Georgia", serif !important;
    font-size: 0.95em !important;
    box-shadow: 0 1px 6px var(--shadow-light) !important;
    border-radius: 6px !important;
    overflow: hidden !important;
}

th {
    background: linear-gradient(135deg, var(--primary-color) 0%, #2d3748 100%) !important;
    color: white !important;
    padding: 14px 16px !important;
    text-align: left !important;
    font-weight: 600 !important;
    border: none !important;
}

td {
    padding: 12px 16px !important;
    border-bottom: 1px solid var(--border-color) !important;
    background: white !important;
}

/* GRÁFICOS */
.figure {
    text-align: center !important;
    margin: 30px 0 !important;
    padding: 20px !important;
    background: white !important;
    border-radius: 8px !important;
    box-shadow: 0 1px 6px var(--shadow-light) !important;
    border: 1px solid var(--border-color) !important;
}

/* CÓDIGO */
pre {
    background: #1a202c !important;
    border-left: 4px solid var(--accent-color) !important;
    padding: 18px !important;
    border-radius: 0 6px 6px 0 !important;
    font-family: "Courier New", monospace !important;
    color: #e2e8f0 !important;
    margin: 20px 0 !important;
}

code {
    background: #edf2f7 !important;
    color: #2b6cb0 !important;
    padding: 3px 6px !important;
    font-family: "Courier New", monospace !important;
    border-radius: 4px !important;
    border: 1px solid #e2e8f0 !important;
}

/* BARRA DE NAVEGACIÓN */
.navbar {
    background: linear-gradient(135deg, var(--primary-color) 0%, #2d3748 100%) !important;
    border: none !important;
}

.navbar-brand {
    color: white !important;
    font-family: "Times New Roman", serif !important;
    font-weight: bold !important;
}

/* RESPONSIVE - DISEÑO ADAPTATIVO */
@media (max-width: 1024px) {
    .main-container {
        flex-direction: column !important;
        gap: 20px !important;
    }
    
    #TOC {
        width: 100% !important;
        min-width: 100% !important;
        max-width: 100% !important;
        position: relative !important;
        top: 0 !important;
        margin-bottom: 20px !important;
        max-height: none !important;
        overflow: visible !important;
    }
    
    .toc-content {
        margin-left: 0 !important;
        padding: 25px !important;
    }
    
    .section.level1 {
        padding: 25px !important;
    }
    
    #TOC::before {
        position: relative !important;
    }
}

@media (max-width: 768px) {
    .main-container {
        padding: 10px !important;
    }
    
    .toc-content {
        padding: 20px !important;
    }
    
    .section.level1 {
        padding: 20px !important;
    }
    
    .abstract {
        padding: 20px !important;
    }
    
    #TOC {
        padding: 20px !important;
    }
    
    #TOC a {
        padding: 8px 10px !important;
        font-size: 0.9em !important;
    }
    
    #TOC ul ul {
        padding-left: 15px !important;
    }
    
    h1 {
        font-size: 2em !important;
    }
    
    h2 {
        font-size: 1.6em !important;
    }
}

/* MEJORAS DE LEGIBILIDAD */
p {
    margin-bottom: 18px !important;
    line-height: 1.8 !important;
    text-align: justify !important;
}

strong {
    color: var(--accent-color) !important;
    font-weight: 600 !important;
}

em {
    color: #4a5568 !important;
}

/* FOOTNOTES */
.footnote {
    font-size: 0.9em !important;
    color: #666666 !important;
    border-top: 1px solid var(--border-color) !important;
    padding-top: 15px !important;
    margin-top: 25px !important;
}

/* ENLACES */
a {
    color: var(--accent-color) !important;
    text-decoration: none !important;
}

a:hover {
    color: var(--primary-color) !important;
    text-decoration: underline !important;
}

/* GARANTIZAR QUE NO HAY CORTES */
html, body {
    overflow-x: hidden !important;
    height: 100% !important;
}

.main-container {
    overflow: visible !important;
    min-height: 100vh !important;
}

/* EFECTO DE SCROLL SUAVE */
html {
    scroll-behavior: smooth !important;
}

/* DESTACADO DE SECCIÓN ACTIVA EN EL ÍNDICE */
#TOC a.active {
    color: var(--accent-color) !important;
    background-color: var(--light-bg) !important;
    border-left: 3px solid var(--accent-color) !important;
    font-weight: 600 !important;
}

/* ANIMACIONES SUAVES */
#TOC, .toc-content, .section {
    transition: all 0.3s ease !important;
}

/* MEJORA VISUAL AL HACER HOVER EN ÍTEMS LARGOS */
#TOC a {
    position: relative !important;
    z-index: 1 !important;
}

#TOC a::before {
    content: '' !important;
    position: absolute !important;
    top: 0 !important;
    left: 0 !important;
    right: 0 !important;
    bottom: 0 !important;
    background: linear-gradient(90deg, transparent 70%, white 100%) !important;
    z-index: -1 !important;
    opacity: 0 !important;
    transition: opacity 0.3s ease !important;
}

#TOC a:hover::before {
    opacity: 1 !important;
}
</style>

```{r setup, include=FALSE}
# Configuración global para el documento
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  fig.align = "center",
  fig.width = 10,
  fig.height = 6,
  out.width = "85%"
)

# Cargar librerías necesarias
required_packages <- c(
  "ggplot2", "dplyr", "tidyr", "knitr", 
  "kableExtra", "scales", "plotly"
)

for(pkg in required_packages) {
  if(!require(pkg, character.only = TRUE, quietly = TRUE)) {
    install.packages(pkg)
    library(pkg, character.only = TRUE)
  }
}

# Configurar tema para gráficos
tema<- theme_minimal() +
  theme(
    text = element_text(family = "Segoe UI", color = "#2d3748"),
    plot.title = element_text(face = "bold", size = 18, hjust = 0.5, color = "#323130"),
    plot.subtitle = element_text(size = 12, hjust = 0.5, color = "#605e5c", margin = margin(b = 15)),
    plot.caption = element_text(size = 10, color = "#605e5c", hjust = 0),
    panel.grid.major = element_line(color = "#f3f2f1"),
    panel.grid.minor = element_blank(),
    plot.background = element_rect(fill = "white", color = NA),
    panel.background = element_rect(fill = "white", color = NA),
    axis.title = element_text(face = "bold", color = "#323130"),
    axis.text = element_text(color = "#605e5c"),
    legend.position = "none"
  )
```

# Introducción

La comprensión de los determinantes del ingreso familiar constituye uno de los elementos centrales para el diseño de políticas públicas orientadas a la reducción de la pobreza y la promoción del desarrollo socioeconómico equitativo. En Colombia, esta problemática adquiere especial relevancia en regiones como el Pacífico, caracterizada por persistentes brechas de desigualdad y condiciones socioeconómicas adversas que contrastan con la riqueza de sus recursos naturales y diversidad cultural.

La región Pacífica colombiana, conformada por los departamentos de Chocó, Cauca, Nariño y Valle del Cauca, alberga aproximadamente 1.5 millones de habitantes, de los cuales el 80% pertenece a comunidades afrodescendientes, 12% a población indígena y 8% a población mestiza. Esta región presenta los mayores niveles de pobreza del país: Chocó registra una incidencia de pobreza monetaria del 67.7%, seguido por Cauca con 58.4% y Nariño con 47.6%, cifras significativamente superiores al promedio nacional. Estas condiciones de vulnerabilidad socioeconómica, sumadas al aislamiento geográfico y la débil presencia institucional, configuran un escenario complejo que requiere análisis detallados de los factores que influyen en la generación de ingresos familiares.

La Encuesta Nacional de Calidad de Vida (ECV) del DANE constituye la herramienta estadística más importante para el análisis de las condiciones socioeconómicas de los hogares colombianos. Esta encuesta, que recopila información sobre diversas dimensiones del bienestar como características de vivienda, educación, salud, fuerza de trabajo y percepciones sobre calidad de vida, proporciona una base de datos robusta para la aplicación de modelos econométricos que permitan identificar los determinantes del ingreso familiar.

Los modelos lineales han demostrado ser herramientas efectivas para el análisis de los determinantes del ingreso familiar, permitiendo cuantificar el efecto de variables socioeconómicas y demográficas sobre los ingresos de los hogares. La literatura internacional y nacional ha documentado consistentemente la relevancia de factores como el nivel educativo, la experiencia laboral, el género, la composición del hogar y las características demográficas en la determinación de los ingresos familiares.

En el contexto específico de la región Pacífica, el análisis de estos determinantes cobra particular importancia debido a las características específicas de su población. Los bajos niveles educativos, la alta informalidad laboral, las estructuras familiares complejas y las condiciones de exclusión social que caracterizan a esta región sugieren que los patrones de determinación del ingreso pueden diferir significativamente de los observados en otras regiones del país.

El presente estudio aplica un modelo de regresión lineal múltiple utilizando los datos de la ECV para la región Pacífica, con el objetivo de analizar la relación entre el ingreso familiar y un conjunto de variables explicativas que incluyen el nivel de estudios, el tiempo trabajado la semana pasada, el sexo del jefe de hogar, la cantidad de personas en el hogar y la edad. Esta selección de variables se fundamenta tanto en la revisión de la literatura especializada como en las características socioeconómicas específicas de la región Pacífica.

# Metodologia

## Descripcion de la encuesta

La Encuesta Nacional de Calidad de Vida (ECV) 2024 tuvo como objetivo general obtener información que permitiera analizar y realizar comparaciones de las condiciones socioeconómicas de los hogares que habitan en el país, las cuales posibilitaran hacer seguimiento a las variables necesarias para el diseño e implementación de políticas públicas.

### Características Metodológicas

**Tipo de investigación**: Encuesta por muestreo

**Diseño muestral**: Muestreo probabilístico, estratificado, de conglomerados y polietápico

**Período de recolección**: Agosto – noviembre de 2024

**Tamaño de la muestra**: 85.387 hogares con encuestas completas donde habitan 228.688 personas

**Cobertura geográfica**: Nacional con desagregaciones para:

### Total nacional y áreas (cabecera y centros poblados-rural disperso)

Bogotá y los treinta y dos departamentos del país con sus respectivas áreas, a excepción de San Andrés, donde solo se cuenta con información de la cabecera municipal

Las nueve regiones tradicionales de la ECV (Antioquia, Bogotá D.C., Caribe, Central, Oriental, Orinoquía-Amazonía, Pacífica, San Andrés y Valle) con sus áreas correspondientes, salvo en el caso de San Andrés

### Unidades estadísticas:

Unidades de observación y análisis: viviendas, hogares y personas

Unidades de muestreo: segmento o conglomerado, conformado en promedio por 10 hogares



## Variables

Las variables utilizadas en el estudio se dividen en dos grupos: dependientes e independientes. En primer lugar, la variable dependiente seleccionada es el Ingreso del hogar, la cual busca medir el nivel monetario total percibido por los miembros del hogar. Este indicador refleja el grado de bienestar económico, la capacidad de consumo y las oportunidades de inversión en educación, salud y vivienda.
En cuanto a las variables independientes, se consideran varios factores cuyo nivel de significancia resulta fundamental para explicar el ingreso. En primer lugar, la educación: las personas con mayor nivel educativo tienden a acceder a empleos mejor remunerados y con mayores oportunidades de ascenso, además de aumentar su productividad y capital humano. Por lo tanto, se espera que a mayor grado educativo alcanzado, el ingreso también sea más alto. Como señalan Psacharopoulos y Patrinos (2018), “por esta razón la educación, y en particular la educación superior, constituyen un elemento importante dentro de los múltiples determinantes del crecimiento económico.”

El sexo constituye un factor determinante en los ingresos del hogar debido a una combinación de factores sociales, económicos y culturales que, en los países latinoamericanos, reproducen desigualdades estructurales entre hombres y mujeres. Como señala la Organización Internacional del Trabajo (2019), “las mujeres siguen ganando aproximadamente un 20 por ciento menos que los hombres, y gran parte de esta brecha no puede explicarse por las características objetivas del mercado laboral” (p. vi).

La región (departamento) corresponde a la Región Pacífica, la cual se escoge como filtro focal del estudio, dado que es una de las zonas de Colombia más diversas en cuanto a clima, población y condiciones económicas, lo que se supone que permitirá realizar un análisis más preciso de las dinámicas económicas regionales.

También se considera que la cantidad de personas en el hogar es una variable influyente en los ingresos, ya que el número de integrantes de un hogar determina tanto su capacidad productiva como sus necesidades económicas. A medida que aumenta el tamaño del hogar, el ingreso total puede incrementarse debido a la presencia de más miembros potencialmente activos en el mercado laboral; sin embargo, este aumento no siempre es proporcional, lo que puede generar una disminución en el ingreso disponible por persona.
Este comportamiento se relaciona con las economías de escala en el consumo y con la estructura demográfica del hogar, factores que condicionan la distribución y el aprovechamiento del ingreso total. De acuerdo con la Comisión Económica para América Latina y el Caribe (CEPAL, 2020), “los hogares más numerosos presentan, en promedio, menores niveles de ingreso por persona y mayores tasas de pobreza, lo que refleja una relación inversa entre tamaño del hogar y bienestar económico” (p. 47). En consecuencia, se espera que, a mayor número de personas en el hogar, el ingreso per cápita tienda a disminuir, reflejando una menor capacidad de consumo individual y mayores presiones presupuestarias para cubrir las necesidades básicas del grupo familiar.

La edad es un factor que se considera relacionado con una mayor experiencia y, por lo tanto, con mayores ingresos. Sin embargo, las jubilaciones pueden afectar el comportamiento lineal que se esperaría, haciendo que este se estabilice o disminuya. Por ello, la edad se reconoce como una variable importante para relacionar con los ingresos, ya que estos tienden a variar a lo largo del ciclo de vida laboral. En Colombia, menos de una cuarta parte de los adultos mayores reciben pensión, y los ingresos laborales siguen siendo la principal fuente de sustento incluso en la vejez (Villar et al., 2016).

**Ecuación del modelo:**
$$ING = \beta_0 + \beta_1(Edad) + \beta_2(Dpto) + \beta_3(Sexo) + \beta_4(C.de personas) + \beta_5(Estudios) + \beta_6() + \epsilon$$

## Base de datos del estudio

Para el presente trabajo, se utiliza la base de datos de la ECV 2024 filtrada específicamente para la región Pacífica, que incluye los departamentos de Chocó, Cauca, Nariño y Valle del Cauca. La base de datos ha sido depurada y contiene únicamente las variables de interés para el modelo de regresión lineal:

```{r base de datos}
#Librerias
library(tidyverse)
library(scales)
library(readxl)
library(ggcorrplot)
library(visreg)
library(nortest)
library(lmtest)
library(ggthemes)
library(ggpmisc)
library(DT)
library(plotly)

datos_hogar=read.csv("datos_hogar.csv", sep = ";") %>% 
  select(DIRECTORIO,I_HOGAR,CANT_PERSONAS_HOGAR) %>% 
  rename("Ingreso del hogar"=2,"Cantidad de personas en el hogar"=3) %>% 
  distinct(DIRECTORIO, .keep_all = TRUE) %>% filter(`Ingreso del hogar`!=0)

educacion=read.csv("educacion.csv", sep= ";") %>% 
  select(DIRECTORIO,P8587) %>% 
  group_by(DIRECTORIO) %>%
  filter(P8587 == max(P8587))  %>%
  ungroup() %>% 
  distinct(DIRECTORIO, .keep_all = TRUE) %>% 
  mutate(
    Estudios = case_when(
      P8587 == 1 ~ "Ninguno",
      P8587 == 2 ~ "Preescolar",
      P8587 == 3 ~ "Primaria (1º-5º)",
      P8587 == 4 ~ "Secundaria (6º-9º)",
      P8587 == 5 ~ "Media (10º-13º)",
      P8587 == 6 ~ "Técnico sin título",
      P8587 == 7 ~ "Técnico con título",
      P8587 == 8 ~ "Tecnológico sin título",
      P8587 == 9 ~ "Tecnológico con título",
      P8587 == 10 ~ "Universitario sin título",
      P8587 == 11 ~ "Universitario con título",
      P8587 == 12 ~ "Postgrado sin título",
      P8587 == 13 ~ "Postgrado con título",
      TRUE ~ "No especificado"
    ),
    CATEGORIA_EDUCATIVA = case_when(
      P8587 %in% 1:2 ~ "Primaria",
      P8587 %in% 3:5 ~ "Secundaria",
      P8587 %in% 6:9 ~ "Técnica/Tecnológica",
      P8587 %in% 10:11 ~ "Universitaria",
      P8587 %in% 12:13 ~ "Postgrado",
      TRUE ~ "Otro"
    ),    CATEGORIA_EDUCATIVA = factor(CATEGORIA_EDUCATIVA,
                                       levels = c("Primaria", "Secundaria", "Técnica/Tecnológica", 
                                                  "Universitaria", "Postgrado")))

caracteristicas_hogar=read.csv("Características_composición.CSV",sep= ";") %>% 
  filter(P6051==1) %>% 
  select(DIRECTORIO,P6020,P6040) %>% rename(Sexo=2,Edad=3)

vivienda=read.csv("Datos de la vivienda.csv",sep=";") %>% 
  select(DIRECTORIO,CLASE,P1_DEPARTAMENTO,P8520S1A1) %>% rename(Ubicacion=2,Departamento=3,Estrato=4) %>% 
  distinct(DIRECTORIO, .keep_all = TRUE) %>% 
  filter(Estrato%in%c(1))

trabajo=read.csv("Fuerza de trabajo.CSV",sep=";") %>% 
  select(DIRECTORIO,P8624,P415,P8634,P6426,P416,P6440,P6460,P6426,P8636) %>% 
  rename(Ingresos_mes=2,Horas_trabajadas_semana=3,"Lugar de trabajo"=4,Tiempo_trabajado=5,
         Semana_horas=6,contrato=7,Tipo_contrato=8,
         meses=8)

Base_datos=caracteristicas_hogar  %>% inner_join(datos_hogar,by="DIRECTORIO") %>% 
  inner_join(vivienda,by="DIRECTORIO") %>% inner_join(educacion,by="DIRECTORIO") %>% inner_join(trabajo,by="DIRECTORIO") %>% 
  filter(Departamento%in%c(76,19,52,27),Edad!=0,Edad>=18,`Ingreso del hogar`<=50000000) %>% 
  select(Departamento,`Ingreso del hogar`,`Cantidad de personas en el hogar`,Sexo,
         Edad,CATEGORIA_EDUCATIVA,Tiempo_trabajado,Horas_trabajadas_semana) %>% mutate(
           Departamento= case_when(
             Departamento == 76 ~ "Valle del Cauca",
             Departamento == 19 ~ "Cauca",
             Departamento == 52 ~ "Nariño",
             Departamento == 27 ~ "Chocó"),
           Sexo= case_when(Sexo==1~ "Hombre",
                           Sexo==2~ "Mujer")) %>% 
  filter(!is.na(Horas_trabajadas_semana)) %>% 
  rename(Estudios=6,"Tiempo total trabajado"=7,"Horas trabajadas la semana pasada"=8)


library(reactable)

reactable(
  Base_datos,
  defaultPageSize = 15,
  showPageSizeOptions = TRUE,
  pageSizeOptions = c(10, 15, 25, 50),
  striped = TRUE,
  highlight = TRUE,
  bordered = TRUE,
  filterable = TRUE,
  showSortable = TRUE,
  wrap = FALSE,
  resizable = TRUE,
  defaultColDef = colDef(
    header = function(value) gsub("_", " ", toupper(value)),
    cell = function(value) format(value, nsmall = 1),
    align = "center",
    minWidth = 120,
    headerStyle = list(background = "#2E86AB", color = "white", fontWeight = "bold")
  ),
  style = list(fontFamily = "Arial, sans-serif", fontSize = "14px"),
  theme = reactableTheme(
    stripedColor = "#f6f8fa",
    highlightColor = "#e8f4fd",
    cellPadding = "8px 12px",
    style = list(fontFamily = "-apple-system, BlinkMacSystemFont, Segoe UI, Helvetica, Arial, sans-serif"),
    searchInputStyle = list(width = "100%", padding = "8px 12px")
  ),
  columns = list(
  )
)
```

# Resultados descriptivos

## Análisis Univariado de las Variables del Modelo

### Caracterización de la Variable Dependiente: Ingreso Total del Hogar

Los resultados del análisis descriptivo para el ingreso total mensual de los hogares colombianos revelan características estructurales relevantes para la comprensión de la distribución del ingreso nacional. En la tabla 1 vemos los analisis descriptivos para esta variable.

```{r tabla-promedios, echo=FALSE, warning=FALSE}
library(dplyr)
library(kableExtra)


tabla_promedios <- Base_datos %>%
  group_by(Departamento) %>%  
  summarise(
    `N Observaciones` = n(),
    `Ingreso Promedio` = mean(`Ingreso del hogar`, na.rm = TRUE),
    `Desv. Estándar Ingreso` = sd(`Ingreso del hogar`, na.rm = TRUE),
    `Ingreso Mínimo` = min(`Ingreso del hogar`, na.rm = TRUE),
    `Ingreso Máximo` = max(`Ingreso del hogar`, na.rm = TRUE),
    .groups = 'drop'
  ) %>%
  mutate(
    `Ingreso Promedio` = round(`Ingreso Promedio`, 2),
    `Desv. Estándar Ingreso` = round(`Desv. Estándar Ingreso`, 2)
  )

tabla_promedios %>%
  kbl(
    caption = "Tabla 1: Estadísticas Descriptivas del ingreso del hogar por Departamento",
    align = c("l", "r", "r", "r", "r", "r"),
    col.names = c("Departamento", "N", "Ingreso promedio",
                  "Desv. Estándar", "Mínimo", "Máximo")
  ) %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "condensed"),
    full_width = FALSE,
    font_size = 14,
    position = "center"
  ) %>%
  row_spec(0, background = "#2b6cb0", color = "white", bold = TRUE) %>%
  column_spec(1, bold = TRUE, width = "3cm") %>%
  column_spec(2:6, width = "2.5cm") %>%  # Corregido: de 2:7 a 2:6
  footnote(
    general = "Fuente: Elaboración propia con base en ECV DANE 2024",
    general_title = "Nota:",
    footnote_as_chunk = TRUE
  )
```

La distribución del ingreso total mensual de los hogares en la región Pacífica de Colombia refleja dinámicas económicas y sociales diferenciadas entre sus departamentos. De acuerdo con los datos de la ECV DANE 2024, presentados en la Tabla 1, se observa que el Valle del Cauca es el departamento con el ingreso promedio más alto ($2,348,430 COP), seguido del Cauca ($2,207,561 COP), Chocó ($1,930,799 COP) y Nariño ($1,767,745 COP). Estas diferencias no solo responden a factores productivos y de acceso a mercados, sino también a condiciones estructurales históricas, como la presencia de economías formales e informales, y niveles desiguales de inversión pública.

La figura 1 permite identificar con claridad un gradiente descendente desde el norte hacia el sur de la región. El Valle del Cauca, con su posición geoestratégica y su base económica diversificada, presenta el mayor ingreso promedio, mientras que Nariño, a pesar de su importancia agrícola, registra el menor. Chocó y Cauca se ubican en posiciones intermedias, aunque con notorias brechas internas, como lo sugieren sus altas desviaciones estándar, lo que evidencia una significativa heterogeneidad en la distribución al interior de estos territorios.



```{r mapa}
library(rnaturalearth)
library(sf)
library(devtools)

colombia <- ne_states(country = "colombia", returnclass = "sf")
pacifica <- colombia %>% filter(name %in% c("Chocó", "Valle del Cauca", "Cauca", "Nariño"))

pacifica_con_ingresos <- pacifica %>%
  left_join(tabla_promedios, by = c("name" = "Departamento"))

azules_puros <- c("#3E4A89", "#31688E", "#26828E", "#1F9E89")


g_mapa=ggplot() +
  geom_sf(data = colombia, fill = "lightgray", color = "white", alpha = 0.5) +
  geom_sf(data = pacifica_con_ingresos, 
          aes(fill = `Ingreso Promedio`), 
          color = "white", size = 0.8) +
   scale_fill_gradientn(
    name = "Ingreso Promedio (COP)",
    labels = dollar_format(prefix = "$", big.mark = ","),
    colors = azules_puros  
  ) +
  labs(
    title = "Mapa de Ingresos Promedio - Región Pacífica de Colombia",
    subtitle = "Distribución del ingreso familiar por departamento",
    caption = "Fuente: ECV DANE 2024"
  ) +theme_void() +
  theme(
    plot.title = element_text(hjust = 0.5, face = "bold", size = 16),
    plot.subtitle = element_text(hjust = 0.5, size = 12),
    legend.position = "right",
    legend.title = element_text(face = "bold")
  )

mapa_interactivo <- ggplotly(g_mapa, tooltip = "text", width = 800, height = 600) %>%
  layout(
    title = list(
      text = "Mapa de Ingresos Promedio - Región Pacífica de Colombia",
      x = 0.5,
      font = list(size = 18, weight = "bold")
    ),
    hoverlabel = list(
      bgcolor = "white",
      bordercolor = "black",
      font = list(size = 12, color = "black")
    ),
    margin = list(t = 60, b = 20, l = 20, r = 20),
    legend = list(
      orientation = "v",
      x = 0.9,
      y = 0.9,
      bgcolor = "rgba(255,255,255,0.8)",
      bordercolor = "lightgray"
    )
  ) %>%
  config(displayModeBar = TRUE, displaylogo = FALSE)

mapa_interactivo
```

## Variables Explicativas Cuantitativas: Caracterización Estadística

### Edad del jefe del hogar

la edad del jefe de hogar se introduce como una variable independiente clave. Las estadísticas descriptivas presentadas en la Tabla 2 revelan la variabilidad transversal entre departamentos, lo cual es fundamental para identificar posibles patrones que el modelo econométrico puede capturar.

```{r tabla edad}
tabla_promedios_edad <- Base_datos %>%
  group_by(Departamento) %>%  
  summarise(
    `Edad Promedio` = mean(Edad, na.rm = TRUE),
    `Desv. Estándar Ingreso` = sd(Edad, na.rm = TRUE),
    `Ingreso Mínimo` = min(Edad, na.rm = TRUE),
    `Ingreso Máximo` = max(Edad, na.rm = TRUE),
    .groups = 'drop'
  ) %>%
  mutate(
    `Edad Promedio` = round(`Edad Promedio`, 1),
    `Desv. Estándar Ingreso` = round(`Desv. Estándar Ingreso`, 2)
  )


tabla_promedios_edad %>%
  kbl(
    caption = "Tabla 2: Estadísticas Descriptivas de la edad del jefe del hogar Agrupadas por departamento",
    align = c("l", "r","r", "r", "r", "r", "r"),
    col.names = c("Código", "Edad Promedio",
                  "Desv. Estándar de la edad", "Mínimo de la edad", "Máximo de la edad")
  ) %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "condensed"),
    full_width = FALSE,
    font_size = 14,
    position = "center"
  ) %>%
  row_spec(0, background = "#2b6cb0", color = "white", bold = TRUE) %>%
  column_spec(1, bold = TRUE, width = "3cm") %>%
  column_spec(2:5, width = "2.5cm") %>%
  footnote(
    general = "Fuente: Elaboración propia con base en ECV DANE 2024",
    general_title = "Nota:",
    footnote_as_chunk = TRUE
  )

```

La dispersión de esta variable, medida através de la desviación estándar, se mantiene relativamente estable entre los departamentos (alrededor de 16 años), indicando una heterogeneidad similar en la composición etaria de los jefes de hogar dentro de cada territorio. El rango de edades abarca desde los 18 años hasta los 99 años, cubriendo así todo el espectro de la vida adulta productiva y post-productiva en la región.

```{r correlacion edad}

scatter_edad <- ggplot(Base_datos, aes(x = Edad,  # Ajusta el nombre de la variable
                                 y = log(`Ingreso del hogar`))) +
  geom_point(
    aes(color = log(`Ingreso del hogar`)),
    alpha = 0.7,
    size = 2.8
  ) +
  geom_smooth(
    method = "loess",  # Usamos loess para mejor ajuste con edad
    color = "#1a3c5f",
    fill = "#6baed6",
    alpha = 0.3,
    linewidth = 1.1,
    se = TRUE
  ) +
  
  # Facetas por departamento
  facet_wrap(~ Departamento, ncol = 2) +
  
  # Leyenda corregida para log(ingreso)
  scale_color_gradient(
    name = "Ingreso Familiar",
    low = "#d0e1f9",
    high = "#1a3c5f",
    labels = function(x) {
      dollar_format(prefix = "$", big.mark = ",")(exp(x))
    },
    breaks = log(c(500000, 1000000, 2000000, 5000000)),
    guide = guide_colorbar(
      barwidth = 12,
      barheight = 0.8,
      direction = "horizontal",
      title.position = "top"
    )
  ) +
  
  # Eje X para edad
  scale_x_continuous(
    name = "Edad del Jefe de Hogar",
    breaks = seq(20, 80, by = 10),
    limits = c(18, 85)  # Ajustar según el rango de edades en tus datos
  ) +
  
  # Títulos y etiquetas
  labs(
    title = "RELACIÓN EDAD DEL JEFE DE HOGAR - INGRESO FAMILIAR POR DEPARTAMENTO",
    subtitle = "Transformación logarítmica aplicada al ingreso | Curva de tendencia LOESS",
    caption = "Fuente: Encuesta Nacional de Hogares | Edad del jefe de hogar vs log(ingreso familiar)"
  ) +
  
  # Tema profesional
  theme_minimal(base_size = 12) +
  theme(
    text = element_text(family = "sans"),
    plot.title = element_text(
      face = "bold",
      size = 14,
      hjust = 0.5,
      margin = margin(b = 10),
      color = "#2c3e50"
    ),
    plot.subtitle = element_text(
      size = 11,
      hjust = 0.5,
      color = "#34495e",
      margin = margin(b = 15)
    ),
    plot.caption = element_text(
      size = 9,
      color = "#7f8c8d",
      hjust = 0.5,
      margin = margin(t = 10)
    ),
    
    # Facetas
    strip.background = element_rect(
      fill = "#3498db",
      color = "#2980b9",
      linewidth = 1
    ),
    strip.text = element_text(
      color = "white",
      face = "bold",
      size = 10
    ),
    
    # Ejes
    axis.title = element_text(face = "bold", size = 11, color = "#2c3e50"),
    axis.text = element_text(size = 9, color = "#34495e"),
    
    # Leyenda
    legend.position = "bottom",
    legend.title = element_text(face = "bold", size = 10, color = "#2c3e50"),
    legend.text = element_text(size = 8, color = "#34495e"),
    
    # Grid
    panel.grid.major = element_line(color = "#ecf0f1", linewidth = 0.4),
    panel.grid.minor = element_line(color = "#f8f9fa", linewidth = 0.2),
    
    plot.margin = margin(15, 15, 15, 15)
  )

# Mostrar gráfica
print(scatter_edad)

```


### Cantidad de personas en el hogar

La cantidad de personas en el hogar se introduce como una variable independiente clave. Las estadísticas descriptivas presentadas en la Tabla 3 revelan la variabilidad transversal entre departamentos, lo cual es fundamental para identificar posibles patrones que el modelo econométrico puede capturar.

```{r cantidad de personas en el hogar}
tabla_promedios_cp <- Base_datos %>%
  group_by(Departamento) %>%  
  summarise(
    `Edad Promedio` = mean(`Cantidad de personas en el hogar`, na.rm = TRUE),
    `Desv. Estándar Ingreso` = sd(`Cantidad de personas en el hogar`, na.rm = TRUE),
    `Ingreso Mínimo` = min(`Cantidad de personas en el hogar`, na.rm = TRUE),
    `Ingreso Máximo` = max(`Cantidad de personas en el hogar`, na.rm = TRUE),
    .groups = 'drop'
  ) %>%
  mutate(
    `Edad Promedio` = round(`Edad Promedio`, 1),
    `Desv. Estándar Ingreso` = round(`Desv. Estándar Ingreso`, 2)
  )


tabla_promedios_cp %>%
  kbl(
    caption = "Tabla 3: Estadísticas Descriptivas de la cantidad de personas en el hogar Agrupadas por departamento",
    align = c("l", "r","r", "r", "r", "r", "r"),
    col.names = c("Departamento", "Cantidad de personas Promedio",
                  "Desv. Estándar", "Mínimo", "Máximo")
  ) %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "condensed"),
    full_width = FALSE,
    font_size = 14,
    position = "center"
  ) %>%
  row_spec(0, background = "#2b6cb0", color = "white", bold = TRUE) %>%
  column_spec(1, bold = TRUE, width = "3cm") %>%
  column_spec(2:5, width = "2.5cm") %>%
  footnote(
    general = "Fuente: Elaboración propia con base en ECV DANE 2024",
    general_title = "Nota:",
    footnote_as_chunk = TRUE
  )

```

La estructura de los hogares en la región Pacífica colombiana presenta notables diferencias en cuanto al número de personas que los conforman. De acuerdo con los datos de la ECV DANE 2024, el departamento de Nariño registra el tamaño promedio de hogar más alto (54.0 personas), seguido muy de cerca por Valle del Cauca (53.1 personas) y Cauca (52.8 personas). En contraste, Chocó presenta un promedio significativamente menor (47.5 personas), lo que sugiere dinámicas familiares y patrones de conformación de hogares diferenciados en este territorio

La dispersión en el tamaño de los hogares, medida a través de la desviación estándar, es relativamente homogénea entre los departamentos, oscilando alrededor de 16 personas. Los valores mínimos y máximos revelan que en toda la región existen hogares que van desde conformaciones unipersonales (1 persona) hasta hogares extensos de más de 90 integrantes, lo que evidencia la diversidad en las estructuras familiares y los arreglos residenciales presentes en la zona.

```{r correlacion cantidad}
scatter_facet_blue <- ggplot(Base_datos, aes(x = `Cantidad de personas en el hogar`, 
                                       y = log(`Ingreso del hogar`))) +
  
  # Capa de puntos con gradiente azul
  geom_point(
    aes(color = `Ingreso del hogar`, fill = `Ingreso del hogar`),
    alpha = 0.7,
    size = 2.8,
    shape = 21,
    stroke = 0.3
  ) +
  
  # Línea de tendencia en azul oscuro
  geom_smooth(
    method = "lm",
    color = "#1a3c5f",  # Azul oscuro
    fill = "#6baed6",   # Azul medio para intervalo de confianza
    alpha = 0.3,
    linewidth = 1.1,
    se = TRUE
  ) +
  
  # Crear facetas por departamento
  facet_wrap(~ Departamento, 
             ncol = 2,
             scales = "fixed") +
  
  # Escalas de color en tonos azules
  scale_color_gradient(
    low = "#d0e1f9",   # Azul muy claro
    high = "#1a3c5f",  # Azul oscuro
    labels = dollar_format(prefix = "$", big.mark = ","),
    guide = guide_colorbar(
      title = "Ingreso Familiar",
      barwidth = 12,
      barheight = 0.8,
      direction = "horizontal"
    )
  ) +
  
  scale_fill_gradient(
    low = "#d0e1f9",
    high = "#1a3c5f",
    guide = "none"  # Solo usamos una leyenda para color
  ) +
  
  # Formato de ejes
  scale_y_continuous(
    labels = dollar_format(prefix = "$", big.mark = ","),
    breaks = breaks_extended(6)
  ) +
  
  scale_x_continuous(
    breaks = function(x) unique(floor(pretty(seq(0, (max(x) + 1) * 1.1))))
  ) +
  
  # Títulos y etiquetas
  labs(
    title = "RELACIÓN TAMAÑO DEL HOGAR - INGRESO FAMILIAR POR DEPARTAMENTO",
    subtitle = "Análisis individual de la correlación en cada departamento - Paleta de azules",
    x = "Cantidad de Personas en el Hogar",
    y = "Ingreso Familiar Mensual",
    caption = "Fuente: Encuesta Nacional de Hogares | Tendencias lineales mostradas en azul oscuro"
  ) +
  
  # Tema profesional con tonos azules
  theme_minimal(base_size = 12) +
  theme(
    # Textos generales
    text = element_text(family = "sans"),
    plot.title = element_text(
      face = "bold",
      size = 16,
      hjust = 0.5,
      margin = margin(b = 10),
      color = "#2c3e50"
    ),
    plot.subtitle = element_text(
      size = 12,
      hjust = 0.5,
      color = "#34495e",
      margin = margin(b = 20)
    ),
    plot.caption = element_text(
      size = 10,
      color = "#7f8c8d",
      hjust = 0.5,
      margin = margin(t = 15)
    ),
    
    # Facetas - fondo azul para títulos
    strip.background = element_rect(
      fill = "#3498db",  # Azul vibrante
      color = "#2980b9",
      linewidth = 1
    ),
    strip.text = element_text(
      color = "white",
      face = "bold",
      size = 11
    ),
    
    # Ejes
    axis.title = element_text(face = "bold", size = 12, color = "#2c3e50"),
    axis.text = element_text(size = 9, color = "#34495e"),
    
    # Leyenda de color
    legend.position = "bottom",
    legend.title = element_text(face = "bold", size = 10, color = "#2c3e50"),
    legend.text = element_text(size = 9, color = "#34495e"),
    
    # Grid en tonos azules suaves
    panel.grid.major = element_line(color = "#ecf0f1", linewidth = 0.4),
    panel.grid.minor = element_line(color = "#f8f9fa", linewidth = 0.2),
    panel.background = element_rect(fill = "white", color = NA),
    plot.background = element_rect(fill = "white", color = NA),
    
    # Márgenes
    plot.margin = margin(20, 20, 20, 20)
  )

# Mostrar gráfica
print(scatter_facet_blue)
```


### Horas trabjadas la semana pasada

```{r cantidad de horas}
tabla_promedios_ch <- Base_datos %>%
  group_by(Departamento) %>%  
  summarise(
    `Edad Promedio` = mean(`Horas trabajadas la semana pasada`, na.rm = TRUE),
    `Desv. Estándar Ingreso` = sd(`Horas trabajadas la semana pasada`, na.rm = TRUE),
    `Ingreso Mínimo` = min(`Horas trabajadas la semana pasada`, na.rm = TRUE),
    `Ingreso Máximo` = max(`Horas trabajadas la semana pasada`, na.rm = TRUE),
    .groups = 'drop'
  ) %>%
  mutate(
    `Edad Promedio` = round(`Edad Promedio`, 1),
    `Desv. Estándar Ingreso` = round(`Desv. Estándar Ingreso`, 2)
  )


tabla_promedios_ch %>%
  kbl(
    caption = "Tabla 4: Estadísticas Descriptivas de las horas trabajas la semana pasada por el jefe del hogar en el hogar Agrupadas por departamento",
    align = c("l", "r","r", "r", "r", "r", "r"),
    col.names = c("Departamento", "Cantidad de horas promedio trabajadas",
                  "Desv. Estándar", "Mínimo", "Máximo")
  ) %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "condensed"),
    full_width = FALSE,
    font_size = 14,
    position = "center"
  ) %>%
  row_spec(0, background = "#2b6cb0", color = "white", bold = TRUE) %>%
  column_spec(1, bold = TRUE, width = "3cm") %>%
  column_spec(2:5, width = "2.5cm") %>%
  footnote(
    general = "Fuente: Elaboración propia con base en ECV DANE 2024",
    general_title = "Nota:",
    footnote_as_chunk = TRUE
  )
```

La Tabla 4 presenta las estadísticas descriptivas de las horas trabajadas por los jefes de hogar en la región Pacífica colombiana, revelando patrones laborales diferenciados entre departamentos. El Valle del Cauca destaca con la jornada laboral promedio más extensa (42.1 horas semanales), superando en aproximadamente 6 horas el promedio de Chocó (33.1 horas), que registra la menor dedicación laboral. Cauca (36.3 horas) y Nariño (34.0 horas) se ubican en posiciones intermedias, mostrando una distribución regional norte-sur en cuanto a intensidad laboral.

La dispersión de los datos, medida através de la desviación estándar (aproximadamente 15 horas en la mayoría de departamentos), indica una heterogeneidad significativa en las jornadas laborales dentro de cada territorio. Los valores extremos son particularmente reveladores: mientras el mínimo de 1 hora sugiere situaciones de subempleo crítico, el máximo de 120 horas semanales evidencia casos de pluriempleo o trabajos con jornadas extensivas, posiblemente en el sector informal.

Estas diferencias en la dedicación laboral pueden estar asociadas a múltiples factores estructurales. La mayor jornada en Valle del Cauca podría relacionarse con su mayor diversificación económica y dinamismo industrial, mientras que las menores jornadas en Chocó podrían reflejar las limitadas oportunidades del mercado laboral formal y la predominancia de actividades económicas estacionales. La distribución horaria observada sugiere que la relación entre horas trabajadas e ingresos del hogar podría no ser lineal, aspecto que el modelo de regresión deberá explorar en profundidad, considerando posibles efectos de rendimientos decrecientes o diferencias en la productividad laboral entre territorios.

```{r correlacion horas}
scatter_horas <- ggplot(Base_datos, aes(x = `Horas trabajadas la semana pasada`, 
                                  y = log(`Ingreso del hogar`))) +
  geom_point(
    aes(color = `Ingreso del hogar`, fill = `Ingreso del hogar`),
    alpha = 0.7,
    size = 2.8,
    shape = 21,
    stroke = 0.3
  ) +
  
  # Línea de tendencia
  geom_smooth(
    method = "lm",
    color = "#1a3c5f",
    fill = "#6baed6",
    alpha = 0.3,
    linewidth = 1.1,
    se = TRUE
  ) +
  
  # Facetas por departamento
  facet_wrap(~ Departamento, 
             ncol = 2,
             scales = "fixed") +
  
  # Escalas de color en tonos azules
  scale_color_gradient(
    low = "#d0e1f9",
    high = "#1a3c5f",
    labels = dollar_format(prefix = "$", big.mark = ","),
    guide = guide_colorbar(
      title = "Ingreso Familiar",
      barwidth = 12,
      barheight = 0.8,
      direction = "horizontal"
    )
  ) +
  
  scale_fill_gradient(
    low = "#d0e1f9",
    high = "#1a3c5f",
    guide = "none"
  ) +
  
  # Formato de ejes
  scale_y_continuous(
    labels = dollar_format(prefix = "$", big.mark = ","),
    breaks = breaks_extended(6)
  ) +
  
  scale_x_continuous(
    breaks = function(x) unique(floor(pretty(seq(0, (max(x) + 1) * 1.1)))),
    labels = function(x) paste(x, "horas")
  ) +
  
  # Títulos y etiquetas
  labs(
    title = "RELACIÓN HORAS TRABAJADAS - INGRESO FAMILIAR POR DEPARTAMENTO",
    subtitle = "Análisis de correlación entre jornada laboral e ingresos familiares",
    x = "Horas Trabajadas Promedio",
    y = "Ingreso Familiar Mensual",
    caption = "Fuente: Encuesta Nacional de Hogares | Líneas muestran tendencia lineal"
  ) +
  
  # Tema profesional
  theme_minimal(base_size = 12) +
  theme(
    text = element_text(family = "sans"),
    plot.title = element_text(
      face = "bold",
      size = 16,
      hjust = 0.5,
      margin = margin(b = 10),
      color = "#2c3e50"
    ),
    plot.subtitle = element_text(
      size = 12,
      hjust = 0.5,
      color = "#34495e",
      margin = margin(b = 20)
    ),
    plot.caption = element_text(
      size = 10,
      color = "#7f8c8d",
      hjust = 0.5,
      margin = margin(t = 15)
    ),
    
    # Facetas
    strip.background = element_rect(
      fill = "#3498db",
      color = "#2980b9",
      linewidth = 1
    ),
    strip.text = element_text(
      color = "white",
      face = "bold",
      size = 11
    ),
    
    # Ejes
    axis.title = element_text(face = "bold", size = 12, color = "#2c3e50"),
    axis.text = element_text(size = 9, color = "#34495e"),
    
    # Leyenda
    legend.position = "bottom",
    legend.title = element_text(face = "bold", size = 10, color = "#2c3e50"),
    legend.text = element_text(size = 9, color = "#34495e"),
    
    # Grid
    panel.grid.major = element_line(color = "#ecf0f1", linewidth = 0.4),
    panel.grid.minor = element_line(color = "#f8f9fa", linewidth = 0.2),
    panel.background = element_rect(fill = "white", color = NA),
    plot.background = element_rect(fill = "white", color = NA),
    
    plot.margin = margin(20, 20, 20, 20)
  )

# Mostrar gráfica
print(scatter_horas)

```



## Variables Categóricas: Distribución de Frecuencias

### Estudios

La tabla 5 revela una estructura formativa predominantemente básica en la región Pacífica colombiana. El nivel de secundaria concentra la mayor proporción con 3,306 casos (79.3%), constituyéndose como la característica educativa más representativa entre los jefes de hogar. Le siguen en importancia la formación técnica o tecnológica con 398 casos (9.5%) y la educación universitaria con 262 observaciones (6.3%).

Los niveles educativos extremos muestran una participación marginal: la educación primaria representa apenas el 3.6% (149 casos), mientras que los estudios de postgrado constituyen el segmento más reducido con solo 54 casos (1.3%). Esta distribución evidencia una pirámide educativa marcadamente amplia en la base y muy estrecha en la cúspide, patrón que sugiere limitaciones en el acceso a educación superior y especializada en la región.

La concentración del 79.3% en el nivel de secundaria implica que la gran mayoría de la población cuenta con educación básica completa, pero presenta una brecha significativa en formación avanzada. Esta característica resulta particularmente relevante para el modelo de regresión, ya que el capital humano, medido a través del nivel educativo, se constituye en un determinante fundamental de la capacidad generadora de ingreso de los hogares.


```{r tabla de estudios}

tabla_resultadoses <- Base_datos %>%
  group_by(Estudios) %>%  # Cambia 'Departamento' por tu variable
  summarise(Frecuencia = n()) %>%
  arrange(desc(Frecuencia)) %>%
  mutate(
    Porcentaje = round((Frecuencia / sum(Frecuencia)) * 100, 1),
    Porcentaje_Acumulado = cumsum(Porcentaje)
  )



tabla_resultadoses %>%
  kable(
    format = "html",
    col.names = c("Estudios", "Frecuencia", "Porcentaje (%)", "Porcentaje Acumulado (%)"),
    align = c("l", "c", "c", "c"),
    caption = "Tabla 5 Distribución de Frecuencias del nivel de estudios"
  ) %>%
 kable_styling(
    bootstrap_options = c("striped", "hover", "condensed"),
    full_width = FALSE,
    font_size = 14,
    position = "center"
  ) %>%
  row_spec(0, background = "#2b6cb0", color = "white", bold = TRUE) %>%
 column_spec(2:4, width = "2.5cm") %>% 
  footnote(
    general = "Fuente: Elaboración propia con base en ECV DANE 2024",
    general_title = "Nota:",
    footnote_as_chunk = TRUE
  )
```

```{r Box plot estudios}

datos <- Base_datos %>%
  mutate(
    Estudios = factor(Estudios, 
                          levels = c("Primaria", "Secundaria", "Técnica/Tecnológica", "Universitaria", "	
Postgrado"),
                          ordered = TRUE)
  )

boxplot_ingresos <- ggplot(datos, aes(x = Estudios, y = log(`Ingreso del hogar`), fill = Estudios)) +
  geom_boxplot(
    alpha = 0.85,
    outlier.shape = 21,
    outlier.size = 2.5,
    outlier.fill = "#e74c3c",
    outlier.alpha = 0.7,
    outlier.stroke = 0.5,
    width = 0.7,
    lwd = 0.6
  ) +
  stat_summary(
    fun = mean,
    geom = "point",
    shape = 23,
    size = 3,
    fill = "white",
    color = "black"
  ) +
  scale_fill_manual(values = c("#3498db", "#2ecc71", "#9b59b6", "#e67e22", "#1abc9c")) +
  labs(
    title = "RELACIÓN ENTRE NIVEL EDUCATIVO E INGRESO FAMILIAR",
    subtitle = "Distribución del ingreso familiar según el nivel de estudios del jefe de hogar\n(escala logarítmica)",
    x = "Nivel de Estudios",
    y = "Ingreso Familiar Mensual (Escala Logarítmica)",
    caption = "Fuente: Encuesta Nacional de Hogares | Elaboración propia"
  ) +tema +
  theme(
    text = element_text(family = "sans"),
    plot.title = element_text(
      face = "bold",
      size = 18,
      hjust = 0.5,
      margin = margin(b = 10)
    ),
    plot.subtitle = element_text(
      size = 12,
      hjust = 0.5,
      color = "gray40",
      margin = margin(b = 20)
    ),
    plot.caption = element_text(
      size = 10,
      color = "gray50",
      hjust = 1,
      margin = margin(t = 15)
    ),
    axis.title = element_text(face = "bold", size = 12),
    axis.text.x = element_text(
      angle = 45,
      hjust = 1,
      size = 10,
      face = "bold"
    ),
    axis.text.y = element_text(size = 10),
    legend.position = "none",
    plot.background = element_rect(fill = "white", color = NA),
    panel.background = element_rect(fill = "white", color = NA),
    panel.grid.major = element_line(color = "gray90", linewidth = 0.3),
    panel.grid.minor = element_blank(),
    plot.margin = margin(30, 30, 30, 30)
  ) +
  annotate(
    "text",
    x = Inf,
    y = -Inf,
    label = "Nota: Escala logarítmica utilizada para mejor visualización de la distribución",
    hjust = 1,
    vjust = -1,
    size = 3,
    color = "gray50",
    fontface = "italic"
  )

print(boxplot_ingresos)

```



### Sexo

La tabla 6 revela una marcada disparidad de género. Los datos muestran una predominancia significativa de hombres como jefes de hogar, representando el 64.6% del total (2,694 casos), mientras que las mujeres constituyen el 35.4% (1,475 casos). Esta relación de aproximadamente 2:1 evidencia una estructura patriarcal aún persistente en la conformación de los hogares de la región.

La sobrerrepresentación masculina en la jefatura del hogar puede asociarse a factores culturales, económicos y sociales característicos de la región Pacífica. Esta distribución desigual refleja patrones tradicionales de organización familiar donde el hombre sigue siendo percibido como el principal proveedor económico del hogar. Para el modelo de regresión, esta variable adquiere especial relevancia al permitir analizar posibles brechas de ingreso asociadas al género del jefe de hogar, así como diferentes patrones de composición y distribución del ingreso familiar según el sexo de quien ejerce la jefatura

```{r tabla sexo}
tabla_resultadossex <- Base_datos %>%
  group_by(Sexo) %>%  
  summarise(Frecuencia = n()) %>%
  arrange(desc(Frecuencia)) %>%
  mutate(
    Porcentaje = round((Frecuencia / sum(Frecuencia)) * 100, 1),
    Porcentaje_Acumulado = cumsum(Porcentaje)
  )



tabla_resultadossex %>%
  kable(
    format = "html",
    col.names = c("Ganero", "Frecuencia", "Porcentaje (%)", "Porcentaje Acumulado (%)"),
    align = c("l", "c", "c", "c"),
    caption = "Tabla 6 Distribución de Frecuencias del genero"
  ) %>%
 kable_styling(
    bootstrap_options = c("striped", "hover", "condensed"),
    full_width = FALSE,
    font_size = 14,
    position = "center"
  ) %>%
  row_spec(0, background = "#2b6cb0", color = "white", bold = TRUE) %>%
 column_spec(2:4, width = "2.5cm") %>% 
  footnote(
    general = "Fuente: Elaboración propia con base en ECV DANE 2024",
    general_title = "Nota:",
    footnote_as_chunk = TRUE
  )
```

```{r Boxplot sexo}
boxplot_ingresosex <- ggplot(Base_datos, aes(x = Sexo, y = log(`Ingreso del hogar`), fill = Sexo)) +
  geom_boxplot(alpha = 0.8, outlier.shape = 21, outlier.size = 2, 
               outlier.fill = "red", outlier.alpha = 0.6)  +
  scale_fill_brewer(palette = "Set2") +
  scale_y_continuous(labels = dollar_format(prefix = "$", big.mark = ",")) +
  labs(
    title = "Distribución del Ingreso Familiar por Nivel de Estudio",
    subtitle = "Análisis de la relación entre educación e ingresos familiares",
    x = "Nivel de Estudio",
    y = "Ingreso Familiar Mensual",
    caption = "Fuente: Datos de la encuesta nacional"
  )  +tema +
  theme(
    text = element_text(family = "sans"),
    plot.title = element_text(
      face = "bold",
      size = 18,
      hjust = 0.5,
      margin = margin(b = 10)
    ),
    plot.subtitle = element_text(
      size = 12,
      hjust = 0.5,
      color = "gray40",
      margin = margin(b = 20)
    ),
    plot.caption = element_text(
      size = 10,
      color = "gray50",
      hjust = 1,
      margin = margin(t = 15)
    ),
    axis.title = element_text(face = "bold", size = 12),
    axis.text.x = element_text(
      angle = 45,
      hjust = 1,
      size = 10,
      face = "bold"
    ),
    axis.text.y = element_text(size = 10),
    legend.position = "none",
    plot.background = element_rect(fill = "white", color = NA),
    panel.background = element_rect(fill = "white", color = NA),
    panel.grid.major = element_line(color = "gray90", linewidth = 0.3),
    panel.grid.minor = element_blank(),
    plot.margin = margin(30, 30, 30, 30)
  )


print(boxplot_ingresosex)


```


#### Departamento

La tabla 7 revela la composición de la muestra estudiada en la región Pacífica colombiana. Cauca representa la mayor proporción con 1,392 observaciones (33.4%), seguido por Nariño con 1,292 casos (31.0%). En conjunto, estos dos departamentos concentran el 64.4% del total de la muestra, indicando una sobrerrepresentación relativa en el estudio. Valle del Cauca contribuye con 782 hogares (18.8%), mientras que Chocó presenta la menor representación con 703 casos (16.9%).

Esta distribución muestral refleja variaciones en el tamaño poblacional de cada departamento y posiblemente en la intensidad del trabajo de campo. La predominancia de Cauca y Nariño en la composición muestral sugiere que los resultados del análisis de regresión podrían estar más influenciados por las características de estos territorios. La representación diferenciada entre departamentos deberá considerarse en la interpretación de los resultados, particularmente en lo relacionado con la generalización de hallazgos a toda la región Pacífica.

```{r tabla de departamento}

tabla_resultados <- Base_datos %>%
  group_by(Departamento) %>%  # Cambia 'Departamento' por tu variable
  summarise(Frecuencia = n()) %>%
  arrange(desc(Frecuencia)) %>%
  mutate(
    Porcentaje = round((Frecuencia / sum(Frecuencia)) * 100, 1),
    Porcentaje_Acumulado = cumsum(Porcentaje)
  )



tabla_resultados %>%
  kable(
    format = "html",
    col.names = c("Departamento", "Frecuencia", "Porcentaje (%)", "Porcentaje Acumulado (%)"),
    align = c("l", "c", "c", "c"),
    caption = "Tabla 7 Distribución de Frecuencias por Departamento"
  ) %>%
 kable_styling(
    bootstrap_options = c("striped", "hover", "condensed"),
    full_width = FALSE,
    font_size = 14,
    position = "center"
  ) %>%
  row_spec(0, background = "#2b6cb0", color = "white", bold = TRUE) %>%
 column_spec(2:4, width = "2.5cm") %>% 
  footnote(
    general = "Fuente: Elaboración propia con base en ECV DANE 2024",
    general_title = "Nota:",
    footnote_as_chunk = TRUE
  )
```






# Resultados del modelo

```{r tabla supuestos, echo= FALSE}
Modelo_final=lm(log(`Ingreso del hogar`)~Sexo+Estudios+
                  `Cantidad de personas en el hogar`+Edad+`Horas trabajadas la semana pasada`+
                  Departamento
                ,Base_datos)
library(broom)

resumen_mod <- broom::tidy(Modelo_final) %>%
  mutate(across(where(is.numeric), ~ round(., 5))) %>%
  rename(`Variable` = term,
    `Coeficiente` = estimate,
    `Error Estándar` = std.error,
    `Estadístico t` = statistic,
    `Valor p` = p.value) %>%
  mutate(Significancia = case_when(
      `Valor p` < 0.001 ~ "***",
      `Valor p` < 0.01  ~ "**",
      `Valor p` < 0.05  ~ "*",
      TRUE ~ "")) %>% 
  mutate(`Valor p` = case_when(
      is.na(`Valor p`) ~ "1",
      `Valor p` < 2e-16 ~ "2e-16",
      TRUE ~ formatC(`Valor p`, format = "e", digits = 3)),
    Sig. = case_when(
      as.numeric(`Valor p`) < 0.001 ~ "***",
      as.numeric(`Valor p`) < 0.01  ~ "**",
      as.numeric(`Valor p`) < 0.05  ~ "*",
      as.numeric(`Valor p`) < 0.1  ~ ".",
      TRUE ~ ""))


resumen_global <- glance(Modelo_final)
summary(resumen_global)


resumen_global <- glance(Modelo_final)
R2 <- round(resumen_global$adj.r.squared, 4)
Fstat <- round(resumen_global$statistic, 2)
pvalor_modelo <- formatC(resumen_global$p.value, format = "e", digits = 2)
N <- resumen_global$df.residual + length(Modelo_final$coefficients)

filas_resumen <- tibble(
  Variable = c("R² ajustado", "F", "Valor p (modelo)", "N"),
  Coeficiente = as.character(c(R2, Fstat, pvalor_modelo, N)),
  `Error Estándar` = as.character(c("", "", "", "")),
  `Estadístico t` = as.character(c("", "", "", "")),
  `Valor p` = as.character(c("", "", "", "")),
  Sig. = as.character(c("", "", "", "")))

resumen_mod_chr= resumen_mod %>%
  mutate_all(as.character)

tabla_final <- bind_rows(resumen_mod_chr, filas_resumen)

tabla_final <- tabla_final %>% 
  select(Variable, Coeficiente, `Error Estándar`, `Estadístico t`, `Valor p`, Sig.)

kbl(tabla_final,
    caption = "Resultados del modelo de regresión lineal múltiple",
    align = c("l", "r", "r", "r", "r", "c"),
    col.names = names(tabla_final)) %>%
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
    full_width = FALSE,
    font_size = 14,
    position = "center") %>%
  row_spec(0, background = "#2b6cb0", color = "white", bold = TRUE) %>%
  column_spec(1, bold = TRUE, width = "4cm") %>%
  column_spec(2:6, width = "2.5cm") %>%
  row_spec((nrow(resumen_mod_chr) + 1):(nrow(resumen_mod_chr) + nrow(filas_resumen)),
           bold = TRUE, italic = FALSE, background = "#e6f0ff") %>%
  footnote(
    general = "Elaboración propia con base en ECV DANE 2024.",
    general_title = "Nota:",
    footnote_as_chunk = TRUE)
```



```{r Grafico plot 1}
library(plotly)
res_df <- augment(Modelo_final)

G_residuos_hist <- ggplot(res_df, aes(x = .resid)) +
  geom_histogram(
    aes(y = after_stat(density)),
    bins = 30,
    fill = "#3498db",
    color = "white",
    alpha = 0.8
  ) +
  # Línea de densidad
  geom_density(linewidth = 1, color = "#e74c3c") +
  # Línea vertical en cero
  geom_vline(
    xintercept = 0,
    color = "#2c3e50",
    linetype = "dashed",
    linewidth = 1
  ) +
  # Línea de distribución normal teórica
  stat_function(
    fun = dnorm,
    args = list(mean = mean(res_df$.resid), sd = sd(res_df$.resid)),
    color = "#27ae60",
    linewidth = 1,
    linetype = "dashed"
  ) +
  labs(
    title = "Distribución de los Residuos del Modelo",
    subtitle = "Validación del supuesto de normalidad de los errores\nLínea roja: densidad observada | Línea verde: distribución normal teórica",
    x = "Residuos", 
    y = "Densidad",
    caption = "Fuente: Elaboración propia con base en ECV DANE 2024"
  ) +
  tema

print(G_residuos_hist)

```

```{r plot 2}
G_qq <- ggplot(res_df, aes(sample = .resid)) +
  geom_qq(alpha = 0.6, color = "#2b6cb0") +
  geom_qq_line(color = "#e74c3c", linewidth = 1) +
  labs(
    title = "Gráfico Q-Q de los Residuos",
    subtitle = "Validación de normalidad - Residuos vs Distribución Normal Teórica",
    x = "Cuantiles Teóricos",
    y = "Cuantiles de los Residuos",
    caption = "Fuente: Elaboración propia con base en ECV DANE 2024"
  ) +
  tema

print(G_qq)
```


```{r plot 3}
G_resid_ajust <- ggplot(res_df, aes(x = .fitted, y = .resid)) +
  geom_point(alpha = 0.6, color = "#2b6cb0") +
  geom_hline(
    yintercept = 0,
    color = "#e74c3c",
    linetype = "dashed",
    linewidth = 1
  ) +
  geom_smooth(
    method = "loess",
    color = "#2c3e50",
    fill = "#bdc3c7",
    alpha = 0.3
  ) +
  labs(
    title = "Residuos vs Valores Ajustados",
    subtitle = "Validación de homocedasticidad y linealidad",
    x = "Valores Ajustados",
    y = "Residuos",
    caption = "Fuente: Elaboración propia con base en ECV DANE 2024"
  ) +
  tema

print(G_resid_ajust)
```

```{r cook}

res_df <- res_df %>%
  mutate(
    cooksd = cooks.distance(Modelo_final),
    obs_number = row_number()
  )

G_cooksd <- ggplot(res_df, aes(x = obs_number, y = cooksd)) +
  geom_point(
    aes(color = cooksd, size = cooksd),
    alpha = 0.7
  ) +
  geom_hline(
    yintercept = 4/nrow(res_df),
    color = "#e74c3c",
    linetype = "dashed",
    linewidth = 1.2
  ) +
  
  # Destacar observaciones influyentes
  geom_point(
    data = res_df %>% filter(cooksd > 4/nrow(res_df)),
    aes(x = obs_number, y = cooksd),
    color = "#e74c3c",
    size = 3,
    shape = 21,
    fill = "transparent",
    stroke = 1.5
  ) +
  geom_text(
    data = res_df %>% 
      filter(cooksd > 4/nrow(res_df)) %>%
      arrange(desc(cooksd)) %>%
      head(5),  # Etiquetar las 5 más influyentes
    aes(label = obs_number),
    vjust = -1,
    hjust = 0.5,
    size = 3,
    fontface = "bold",
    color = "#c0392b"
  ) +
  scale_color_gradient(
    name = "Distancia de Cook",
    low = "#3498db",
    high = "#e74c3c",
    guide = guide_colorbar(
      barwidth = 10,
      barheight = 0.6,
      direction = "horizontal"
    )
  ) +
  
  scale_size_continuous(
    name = "Influencia",
    range = c(1, 4),
    guide = "none"
  ) +
  labs(
    title = "Análisis de Observaciones Influentes - Distancia de Cook",
    subtitle = paste(
      "Línea roja: umbral de influencia (4/n =", 
      round(4/nrow(res_df), 4), 
      ")\nPuntos rojos: observaciones potencialmente influyentes"
    ),
    x = "Número de Observación",
    y = "Distancia de Cook",
    caption = paste(
      "Fuente: Elaboración propia con base en ECV DANE 2024 |",
      "Total de observaciones:", nrow(res_df)
    )
  ) + tema

print(G_cooksd)

```



```{r correlacion}
num_vars <- Base_datos %>% 
  select_if(is.numeric)

corr_result <- Hmisc::rcorr(as.matrix(num_vars))

cor_y <- corr_result$r[, "Ingreso del hogar"]
pval_y <- corr_result$P[, "Ingreso del hogar"]

Modelop=lm(`Ingreso del hogar`~`Tiempo total trabajado`,Base_datos)


tabla_corr <- tibble(
  Variable = names(cor_y),
  `Coef. de Pearson (r)` = round(cor_y, 3),
  `Valor p` = pval_y) %>%
  filter(Variable != "Ingreso del hogar") %>%
  mutate(
    `Valor p` = case_when(
      is.na(`Valor p`) ~ NA_character_,
      `Valor p` < 2e-16 ~ "2e-16",
      TRUE ~ formatC(`Valor p`, format = "e", digits = 3)),
    Significativo = ifelse(as.numeric(pval_y[match(Variable, names(pval_y))]) < 0.05, "Sí", "No"))

tabla_corr %>%
  kbl(caption = "Coeficiente de Correlación de Pearson con la Variable Dependiente",
    align = c("l", "r", "r", "c"),
    col.names = c("Variable", "Coef. de Pearson (r)", "Valor p", "Significativo")) %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "condensed"),
    full_width = FALSE,
    font_size = 14,
    position = "center") %>%
  row_spec(0, background = "#2b6cb0", color = "white", bold = TRUE) %>%
  column_spec(1, bold = TRUE, width = "3cm") %>%
  column_spec(2:4, width = "2.5cm") %>%
  footnote(general = "Elaboración propia con base en ECV DANE 2024",
    general_title = "Nota:")
```


# Prediccion


# Conclusiones 

# Bibliografia
